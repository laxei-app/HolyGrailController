entity部分やデータに日本語が入っているところを英語、もしくはコードに置き換える。

■該当箇所で日本語を使っているところ
1)保存・通信・表示に乗るデータ(8か所)
2)UI へ通知される文言(10か所)
3)ログ本文(40か所・45リテラル)
4)診断出力(13か所・17リテラル)

■修正方法
方針としてはすべて英語にしましょう。
ログ出力は英語のままでいいです。
どの言語にしても英語で出るようにします。
entityで作るデータでUIに出る物はコードで保持することとする。
UI側で文字列にして表示する。
「保存・通信・表示に乗るデータ(8か所)」
コード上で持つデータは英語にしてください。
英語は初期値だけになるはずです。
ユーザーが変更した場合はその言語で保持します。
この方針で修正するのがとても大変、とか矛盾があるとかあったら教えてください。

■回答
以下方法で行けますか？
① 撮影制御方法の名前は「ラベル」ではなく「キー」でした
初期値の場合は英語のまま表示する。
ユーザーが直したらそのままそれを表示する、でいいです。
ログに撮影制御方法を出していますがこれもユーザーが日本語に直したなら日本語でそのまま出力します。

② 1か所だけ、日本語を残さないといけません
移行処理は削除していいです。
まだ、リリース前の段階なので今の状態で過去データの互換性は考慮しなくていいです。
古いデータは全部削除でいいです。

③ UI通知は仕組みができています。ただし1件だけ厄介です
厄介部分はログに出力することして上位には失敗したとだけ返す方針にしましょう。

④ ログが英語になると、過渡期のログが混在します
問題ありません。

■確認
確認させてください。
1)etpEdge.cpp の既定名 "エッジ端末" を "Edge" にすると、エッジのLCDで文字化けしなくなります。
エッジには日本語フォントを入れていないので、既定名のまま使うと現状は読めない表示になっていたはずです。

→ エッジ端末の初期の名前が「エッジ端末」という日本語だったという事？

2)③ UI通知は仕組みができています。ただし1件だけ厄介です
→ これの厄介なところについて。
　UIに出るのはどんな時？失敗した時？

