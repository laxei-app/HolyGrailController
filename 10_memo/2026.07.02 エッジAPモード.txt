エッジ端末APモード

■検討してもらった
1. モード: 純AP(野外・ルーター無)前提でよいか、AP+STA同時も要るか
2. AP資格の配布: 固定SSID/パス+LCDにQR表示 か、BLEプロビジョニングを拡張して設定式にするか
3. カメラ発見: SSDPのAP対応を狙うか、最初から「AP接続局の列挙→CCAPI直叩き」を本命にするか(野外・少数機なら後者が堅実)

この3点の方針が決まれば、実装計画(フェーズ分け)に落とし込めます。今回は検討のみで、実装はまだ行っていません。

■回答
屋外で別のトラベルルーターを持って行かなくても現状の機能を実現させたいという事です。
使用するシナリオとしては、
・エッジ端末を AP としてスマホとカメラが接続する。
・エッジ端末は現状のアプリとしても機能する。
・スマホから撮影計画をエッジ端末に転送しエッジ端末経由で撮影を開始する。
・スマホで撮影中の状況確認ができる。

カメラ接続は2台はできるようにしたい。

という使い方で、以下方法でできますかね。
1.純APで良い、AP+STAは不要。
2. AP資格の配布: 固定SSID/パス+LCDにQR表示
3.SSDP不要

まだ、実装はしないでくださいね。
問題ないだろうかという相談です。

■claude の回答
まとめ(相談への回答)

- ご提示の 1(純AP)・2(固定+QR)・3(SSDP不要)は 問題なし。3はむしろ推奨。
- 「エッジがAP兼アプリ/計画転送→エッジ撮影/スマホ監視」は 既存構造の延長で実現可能。
- 唯一の本質的作業は「2台“同時精密”のためのカメラ別I/O並列化(netThread直列の解消)」。
ここだけ設計・実機検証が要る。

実装はまだしません。
次に進めるなら、決めたいのは:2台目も15ms精度が要るか(=I/O並列化を入れるか)、
そして 計画↔カメラの割り当てUI(2計画運用か、1計画に2カメラか) の2点です。
ここが決まれば、フェーズ分けした実装計画に落とせます。

■自分の回答
1つの計画で1台のカメラです。
2つの計画を撮影開始して2台のカメラを制御します。
ここま今までの設計のまま行けると思っています。

15ms精度は欲しいです。
1台の撮影の空き時間があるのでタイミングをずらせば15ms周期は行けると思っています。
あと、エッジ端末のAPモード/他のAPに接続、はスマホから設定して切り替えられることです。
可能ですよね。

■追加の質問
もうちょっと待ってください。
追加の質問です。

まだ何もエッジ端末に設定されていない状態で AP モードにしようとすると BLE プロビジョニング拡張でやる必要があるんですかね。
いったん既存のネットワークに接続させてから AP モードにするという手順にすれば楽に実装できますか。
ユーザーの設定の手間が簡単な方がいいんですが、推奨はどんな方法でしょう？

■追加の問い合わせ
AP/STA切り替えはスマホからやりたいです。
スマホと接続できていれば切り替えは容易ですよね。
一番最初の設定をAPモードにすると次に STAモードに切り替える場合SSIDとパスワードが盗まれる可能性がありますよね。
つながっていれば暗号化で通信できます？
現状は BLE プロビジョニングが実装されてるので拡張した方が楽なのかな。
ちょっとまとまっていませんがアドバイスください。

